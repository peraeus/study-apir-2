name: Continuous Delivery

on:
    push:

jobs:

    push_to_docker_hub:
        runs-on: ubuntu-latest
        steps:
            -   uses: actions/checkout@v4
            -   name: Imprimir mensagem
                run: echo "Olá, mundo!"
            -   name: Step 2
                run: |
                    echo "Nova i"
                    ls
            -   name: One Piece
                run: echo "kaizoku oniorenaru tokuta"
